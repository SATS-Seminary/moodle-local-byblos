/**
 * Share page — swap the sharevalue picker to match the selected sharetype.
 *
 * Only the picker matching the current sharetype is named "sharevalue" and
 * enabled; the others are hidden and disabled so the form posts a single
 * consistent value. Public type submits no sharevalue.
 *
 * @module     local_byblos/share
 * @copyright  2026 South African Theological Seminary
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_byblos/share",[],(function(){function apply(selected){document.querySelectorAll(".byblos-share-picker").forEach((function(group){var matches=group.dataset.sharetype===selected;group.classList.toggle("d-none",!matches);var select=group.querySelector('select[data-name="sharevalue"]');select&&(matches?(select.name="sharevalue",select.disabled=!1):(select.removeAttribute("name"),select.disabled=!0))}))}return{init:function(){var typeSelect=document.getElementById("byblos-sharetype");typeSelect&&(typeSelect.addEventListener("change",(function(){apply(typeSelect.value)})),apply(typeSelect.value))}}}));

//# sourceMappingURL=share.min.js.map